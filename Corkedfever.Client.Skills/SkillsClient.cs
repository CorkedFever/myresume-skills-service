namespace Corkedfever.Client.Skills
{
    public class SkillsClient : SkillsClientBase
    {
        public SkillsClient(string baseUrl) : base(baseUrl)
        {
            BaseUrl = baseUrl;
        }
    }
}
